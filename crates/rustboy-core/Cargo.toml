[package]
name = "rustboy-core"
description = "Game Boy Color emulation core: CPU, PPU, APU, bus. No I/O, no dependencies."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true

# Intentionally empty. The core must stay portable to wasm32 and to any
# platform layer; anything that touches files, time or the screen belongs
# in a host crate.
[dependencies]
